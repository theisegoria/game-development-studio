name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['22', '24']

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint

      # The closed direct Node runtime stages a Mach-O library closure and is
      # deliberately macOS-only. Its dedicated test runs in macos-app below;
      # the Ubuntu matrix continues to cover every cross-platform test.
      - name: Run cross-platform TypeScript tests
        run: npm test -- --exclude tests/cli-runtime-payload.test.ts --no-file-parallelism
      - run: npm run verify

      - name: Audit the publish roster
        run: npm pack --dry-run --json

  probe-sdk:
    # The C probe SDK is shipped as source and compiled by the engine's build,
    # never by npm. This proves it compiles under the strictest flags it
    # claims, on both platforms it targets, and that a bundle it writes is
    # accepted by the TypeScript harness -- which is the only validation it
    # has, deliberately: golden files generated by the implementation under
    # test always pass.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      - name: Compile the SDK and example under -std=c99 -Wall -Wextra -Werror
        run: cc -std=c99 -Wall -Wextra -Werror probe/c/gdprobe.c probe/examples/minimal/main.c -o /tmp/gdprobe-minimal

      - name: Run the example outside the harness (must exit 0 and do nothing)
        run: /tmp/gdprobe-minimal

      # The OpenGL example builds on both platforms: CGL on macOS, surfaceless
      # EGL on Linux, where llvmpipe is the software lane and is declared so.
      - name: Install Mesa for the surfaceless OpenGL example
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libegl1-mesa-dev libgl1-mesa-dev libegl-mesa0 libgl1-mesa-dri
      - name: Build and run the OpenGL example outside the harness
        run: |
          probe/examples/opengl/build.sh
          LIBGL_ALWAYS_SOFTWARE=1 probe/examples/opengl/gdprobe-opengl

      - name: Drive the SDK through the real harness
        run: npm run build && npx vitest run tests/probe-sdk.test.ts --no-file-parallelism

      - name: Prove the SDK tests actually ran rather than skipped
        run: |
          npx vitest run tests/probe-sdk.test.ts --no-file-parallelism --reporter=json --outputFile=/tmp/probe.json
          node -e "
            const r = require('/tmp/probe.json');
            const passed = r.numPassedTests, skipped = r.numPendingTests;
            console.log('passed', passed, 'skipped', skipped);
            if (passed < 6) { console.error('probe SDK tests did not run'); process.exit(1); }
          "

      # A hosted macOS runner has a paravirtual Metal device: real GPU
      # execution evidence, not target-hardware timing. The test declares
      # the run attested for its own duration because it asserts the SDK's
      # CLAIM about provenance, not the runner's hardware. Nothing this job
      # produces is admitted anywhere else.
      - name: Drive the Metal example on the runner's GPU
        if: runner.os == 'macOS'
        run: |
          npx vitest run tests/probe-metal.test.ts --no-file-parallelism --reporter=json --outputFile=/tmp/metal.json
          node -e "
            const r = require('/tmp/metal.json');
            const passed = r.numPassedTests, skipped = r.numPendingTests;
            console.log('passed', passed, 'skipped', skipped);
            if (passed < 4) { console.error('Metal example tests did not run'); process.exit(1); }
          "

  wgpu-example:
    # The Rust example is compiled here and nowhere in the harness's own test
    # suite, which must run anywhere a C compiler does. lavapipe makes it the
    # software lane: a CPU adapter, declared so, attesting nothing.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install lavapipe
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends mesa-vulkan-drivers libvulkan1
      - name: Build the wgpu example against the C SDK
        run: cargo build --release --manifest-path probe/examples/wgpu/Cargo.toml
      - name: Run it outside the harness on lavapipe
        run: probe/examples/wgpu/target/release/gdprobe-wgpu 0

  software-lane:
    # The software-raster lane's one real promise, proven through the shipped
    # CLI on both platforms: the same scenario captured twice is byte-identical,
    # so `visual compare --threshold 0` is a hard regression gate -- and the
    # harness refused every GPU and timing claim the lane could have made.
    # The script does not skip: no compiler is a failure, not a pass.
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci

      - name: Capture twice on the CPU and prove the outputs are byte-identical
        run: npm run verify:software-lane

  blender:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Install Blender
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends blender python3-numpy
          blender --version | head -1

      - name: Refuse to continue if Blender is not discoverable
        run: |
          node -e "
          import('./dist/util/blender.js').then(({ findBlender }) => {
            const found = findBlender();
            if (!found) {
              console.error('Blender not discoverable - gated tests would skip');
              process.exit(1);
            }
            console.log('Blender discovered at ' + found);
          });
          "

      - name: Prove Blender can import glTF
        run: >-
          blender --background --factory-startup
          --python-expr "import bpy, numpy; bpy.ops.import_scene.gltf; print('glTF importer and numpy are available')"

      - name: Run the Blender-backed tests
        run: npx vitest run tests/normalize.test.ts

      - name: Prove the Blender-gated tests actually ran
        run: |
          npx vitest run tests/normalize.test.ts --reporter=json --outputFile=blender-results.json
          node .github/scripts/assert-blender-tests-ran.mjs blender-results.json

  macos-app:
    runs-on: macos-26
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '25.2.1'
          cache: npm

      - name: Install Node build dependencies
        run: npm ci

      # This is the admissible platform for the direct runtime's Mach-O
      # closure. Do not turn it into a Linux skip without this macOS gate.
      - name: Run macOS-only closed CLI runtime payload tests
        run: npm run test:runtime

      - name: Report the native toolchain
        run: |
          sw_vers
          xcodebuild -version
          swift --version

      - name: Audit binary-only release packager fixtures
        run: ./script/package_macos_release.sh self-test

      - name: Build the macOS 26 app
        run: swift build --package-path apps/macos/GameDevelopmentStudio

      - name: Test the app model, process boundary, and credential boundary
        run: swift test --package-path apps/macos/GameDevelopmentStudio

      - name: Stage and ad-hoc sign the local app bundle
        run: ./script/build_and_run.sh --build-only

      - name: Validate the staged bundle
        run: |
          plutil -lint apps/macos/GameDevelopmentStudio/dist/GameDevelopmentStudio.app/Contents/Info.plist
          codesign --verify --deep --strict apps/macos/GameDevelopmentStudio/dist/GameDevelopmentStudio.app
